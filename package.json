{
  "name": "aikya-courses",
  "version": "1.0.0",
  "scripts": {
    "build": "cd client && npm install --include=dev && node node_modules/vite/bin/vite.js build && cd ../server && npm install",
    "start": "node server/server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
